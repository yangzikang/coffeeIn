<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.atom.coffee">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CoffeeBean" />
        <activity android:name=".kinds_tab" />
        <activity android:name=".kinds_car" />
        <activity android:name=".SortActivity" />
        <activity android:name=".OriginActivity" />
        <activity android:name=".IngredientActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".ProcessActivity" />
        <activity android:name=".HuActivity"></activity>
    </application>

</manifest>